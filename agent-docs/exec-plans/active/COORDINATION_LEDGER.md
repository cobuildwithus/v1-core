# Coordination Ledger (Active Only)

Use this file only for currently active coding work. Keep it minimal and current.

## Open Entries

| Agent/Session | Task | Files in Scope | Symbols (add/rename/delete) | Dependency Notes | Updated (YYYY-MM-DD) |
| --- | --- | --- | --- | --- | --- |
| codex-main | Trim managed-controller unused config/storage surface | `src/interfaces/IManagedBudgetController.sol`, `src/interfaces/IManagedBudgetControllerStackDeployer.sol`, `src/goals/ManagedBudgetController.sol`, `src/goals/ManagedBudgetControllerStackDeployer.sol`, `src/goals/library/GoalFactoryManagedPresetDeploy.sol`, `src/goals/GoalFactory.sol`, managed-focused tests/docs | Delete managed `budgetAllocationLedger`/`underwriterSlasherRouter`/`budgetPremiumPpm`/`budgetSlashPpm` init+getter surface; remove managed deployer args for ledger/router/slash; remove managed noop gate-policy preset wiring in favor of explicit zero gate policy; simplify post-trim init plumbing only where behavior is unchanged | Must layer cleanly on current `GoalFactory.sol` open-budget-gate diff; do not touch active `NullPremiumEscrow` getter-prune work | 2026-03-12 |
| codex-main | Orchestrate codex-4 simplification worker batch and reserve safe per-target ownership | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `agent-docs/exec-plans/active/2026-03-12-simplification-worker-batch.md` | Add worker-batch plan doc; add reserved worker ownership rows for targets 1-9; no production-symbol changes | Parent-managed orchestration only; child workers must not edit ledger unless scope must change and should otherwise report blockers | 2026-03-12 |
| codex-worker-t1-spend-policy | Unify duplicated spend-policy validation probe behind one shared helper | `src/goals/TreasuryBase.sol`, `src/goals/GoalFactory.sol`, `src/tcr/BudgetTCR.sol`, optional shared helper library, related tests | Add shared spend-policy validation helper; replace duplicated probe logic at three call sites; no behavior changes to caller-specific custom errors | Blocked while active `GoalFactory.sol` and `BudgetTCR.sol` ownership remains open; worker should fail closed and report a plan if conflict persists | 2026-03-12 |
| codex-worker-t2-success-assertion | Extract stateless/common success-assertion lifecycle handling shared by both treasuries | `src/goals/GoalTreasury.sol`, `src/goals/BudgetTreasury.sol`, optional shared treasury helper library, related treasury tests | Add shared success-assertion decision/helper surface; reduce duplicate skeleton logic without changing storage layout or lifecycle semantics | Can proceed only inside listed treasury scope; do not expand into unrelated goal/budget files without parent approval | 2026-03-12 |
| codex-worker-t3-deploy-goal-flatten | Flatten `GoalFactory._deployGoal` into validation, plan resolution, and preset execution helpers | `src/goals/GoalFactory.sol`, optional `src/goals/library/**` helper, related deployment tests | Add `DeploymentPlan`/helper split as needed; reduce nested preset branching; preserve deploy invariants and post-condition checks | Blocked while active `GoalFactory.sol` ownership remains open; worker should fail closed and report a plan if conflict persists | 2026-03-12 |
| codex-worker-t4-managed-active-derived | Derive managed budget active state from index mapping instead of storing duplicate bool | `src/goals/ManagedBudgetController.sol`, `src/interfaces/IManagedBudgetController.sol`, related managed-controller tests | Delete stored `BudgetDeployment.active` field if safe; derive `active` from `_activeBudgetIndexPlusOne`; update views/tests consistently | Blocked while active managed-controller trim ownership remains open; worker should fail closed and report a plan if conflict persists | 2026-03-12 |
| codex-worker-t5-child-sync-debt-derived | Remove redundant `ChildSyncDebt.exists` storage and derive existence from payload state | `src/hooks/GoalFlowAllocationLedgerPipeline.sol`, related hook/ledger tests | Delete stored `exists` field; derive external `exists` from nonzero child flow payload; preserve open/clear semantics | Parent batches this before target 6 because both touch `GoalFlowAllocationLedgerPipeline.sol` | 2026-03-12 |
| codex-worker-t6-child-sync-reasons | Canonicalize child-sync reason strings to one source of truth | `src/hooks/GoalFlowAllocationLedgerPipeline.sol`, `src/library/GoalFlowLedgerMode.sol`, related tests | Delete duplicated `_CHILD_SYNC_*` constants; reuse canonical `GoalFlowLedgerMode` reason surface; preserve emitted/exposed strings | Wait for target 5 worker to finish or report no-op before touching the shared pipeline file | 2026-03-12 |
| codex-worker-t7-goal-treasury-trim | Remove no-op GoalTreasury wrappers and unused source-token parameter | `src/goals/GoalTreasury.sol`, related treasury tests | Inline trivial wrappers; delete unused `sourceToken` parameter if safe; update call sites/tests consistently | Wait for target 2 worker to finish or report no-op before touching `GoalTreasury.sol` | 2026-03-12 |
| codex-worker-t8-budget-topology-dedupe | Deduplicate shared budget-topology reader/lookup logic between controller and TCR | `src/goals/ManagedBudgetController.sol`, `src/tcr/BudgetTCR.sol`, optional shared helper library, related tests | Add shared helper for common topology/read logic; reduce duplication while preserving extra TCR-specific fields | Blocked while active managed-controller and open-budget-gate ownership remain open; worker should fail closed and report a plan if conflict persists | 2026-03-12 |
| codex-worker-t9-dead-code-prune | Remove repo-local unused goal/interface helper surface that production contracts do not reference | `src/goals/library/GoalSpendPatterns.sol`, `src/interfaces/IHasStakeVault.sol`, `src/interfaces/IGoalLedgerStrategy.sol`, `src/interfaces/external/uniswap/permit2/IEIP712.sol`, any direct in-repo import sites/tests/docs if found | Delete unused files/imports if zero in-repo references; otherwise downgrade to blocked report with reference list | Must confirm zero in-repo references before deletion; do not assume external raw-path consumers are irrelevant without documenting the tradeoff | 2026-03-12 |
| codex-main | Add canonical goal/community exit settlement router | `src/juicebox/CobuildCommunityTerminal.sol`, `src/juicebox/CobuildExitRouter.sol`, `src/interfaces/ICobuildCommunityTerminal.sol`, `test/juicebox/helpers/MockTerminalStore.sol`, `test/juicebox/CobuildCommunityTerminal.t.sol`, `test/juicebox/CobuildExitRouter.t.sol`, matching architecture/protocol docs | Add `CobuildCommunityTerminal.cashOutTokensOf`; add `CobuildExitRouter` exit entrypoints and inferred lineage helpers; add `ICobuildCommunityTerminal` boundary interface; extend test store cash-out mocks and add router/terminal cash-out coverage | Preserve existing community pay-routing behavior; keep cross-domain boundary explicit via `src/interfaces/**`; do not overwrite unrelated dirty doc/worktree changes while updating exit-path docs | 2026-03-12 |
## Rules

1. Add a row before your first code edit for every coding task (single-agent and multi-agent).
2. Update your row immediately when scope or symbol-change intent changes.
3. Before deleting or renaming a symbol, check this table for dependencies.
4. Delete your row as soon as the task is complete or abandoned.
5. Leave only the header and empty table when there is no active work.
