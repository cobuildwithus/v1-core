# Coordination Ledger (Active Only)

Use this file only for currently active coding work. Keep it minimal and current.

## Open Entries

| Agent/Session | Task | Files in Scope | Symbols (add/rename/delete) | Dependency Notes | Updated (YYYY-MM-DD) |
| --- | --- | --- | --- | --- | --- |
| managed-preset-coordination | Freeze managed-goal preset names, boundaries, and stream ownership | `agent-docs/exec-plans/active/2026-03-11-managed-goal-preset-master-plan.md`; `agent-docs/references/module-boundary-map.md`; `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add `2026-03-11-managed-goal-preset-master-plan.md`; freeze names `IBudgetController`, `IBudgetGatePolicy`, `ManagedBudgetController`, `NullPremiumEscrow`, `IGoalScopedAllocationStrategy`, `SingleAllocatorStrategy`; no Solidity edits | Coordination-only stream; future implementation agents must add separate rows before touching code and must follow the frozen boundaries in the master plan | 2026-03-11 |
| stream-3-budget-gate-policy-extraction | Split budget routing sync from budget enablement gating by introducing reusable gate-policy hooks and concrete open/managed policies | `agent-docs/exec-plans/active/2026-03-11-stream3-budget-gate-policy-extraction.md`; `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`; `src/interfaces/IBudgetGatePolicy.sol`; `src/goals/policies/StakeCoverageGatePolicy.sol`; `src/goals/policies/NoopBudgetGatePolicy.sol`; `src/goals/policies/library/BudgetGatePolicyHook.sol`; `src/tcr/BudgetTCR.sol`; `src/tcr/interfaces/IBudgetTCR.sol`; `src/tcr/storage/BudgetTCRStorageV1.sol`; `src/tcr/library/BudgetTCRCreditCapActions.sol`; `test/BudgetTCRCreditLineGating.t.sol`; `test/tcr/BudgetTCRRunwayCapEnforcement.t.sol`; targeted policy-hook tests as needed | add `IBudgetGatePolicy`, `StakeCoverageGatePolicy`, `NoopBudgetGatePolicy`, `BudgetGatePolicyHook`; extract stake-backed cap result path out of `BudgetTCRCreditCapActions`; add `budgetGatePolicy` deployment config + storage wiring | Final hook shape: controller builds `IBudgetGatePolicy.SyncContext { itemID, goalFlow, childFlow, budgetTreasury, coverageSource, coverageToCreditPpm }`; policy returns `SyncResult { shouldSetRecipientEnabled, recipientEnabled, failures[] }`; `BudgetGatePolicyHook` supplies both the init-time zero-context policy validation probe (`supportsBudgetGatePolicy`) and runtime revert normalization into `failures[]`; controller emits failures and owns `setRecipientEnabled`. `itemID` is intentionally retained in the shared context for cross-stream API stability even though the current Stream 3 policies do not read it yet. Stream 5 should call the shared hook plus `NoopBudgetGatePolicy` and the same validation probe instead of inlining managed gating. Stream 6 should replace the temporary open-preset `budgetGatePolicy = address(0)` default path with explicit preset wiring for both open and managed controllers. | 2026-03-11 |
| stream4-budget-stack-modularization | Modularize budget stack deployer/config for strategy, child `recipientAdmin`, premium module, and optional mechanism deployment; add `NullPremiumEscrow` wiring surface | `agent-docs/exec-plans/active/2026-03-11-stream4-budget-stack-modularization.md`; `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`; `src/tcr/BudgetTCRDeployer.sol`; `src/tcr/interfaces/IBudgetTCRStackDeployer.sol`; `src/tcr/interfaces/IBudgetTCRDeployer.sol`; `src/tcr/interfaces/IBudgetTCRChildFlowStrategyFactory.sol`; `src/tcr/library/BudgetTCRStackActions.sol`; `src/tcr/library/BudgetTCRStackDeploymentLib.sol`; `src/tcr/interfaces/IBudgetTCR.sol`; `src/goals/BudgetTreasury.sol`; `src/interfaces/IPremiumEscrow.sol`; `src/goals/NullPremiumEscrow.sol`; `test/BudgetTCRDeployments.t.sol`; `test/BudgetTCRManagedStackDeployments.t.sol`; `test/BudgetTCRBudgetTreasuryInvariant.t.sol` | add deployer stack-module config structs + child-strategy factory hook; add `NullPremiumEscrow`; update budget-stack prep/deploy results for optional mechanism topology and zero mechanism addresses; keep `premiumEscrow` naming intact | Final deployer surface is `StackModuleConfig { childFlowStrategyMode, childFlowStrategyTarget, mechanismLayerMode, childFlowRecipientAdmin, premiumEscrowImplementation, requireZeroPremiumAndSlashRates }` plus `PreparationResult { strategy, budgetTreasury, premiumEscrow, childFlowRecipientAdmin, allocationMechanism }`. Open preset remains legacy `initialize(...)` => shared router + real premium escrow + `AllocationMechanismTCR` + `requireZeroPremiumAndSlashRates = false`. Managed preset is `initializeWithConfig(...)` => fixed/factory child strategy + explicit child `recipientAdmin` (Safe in v1) + `NullPremiumEscrow` + `MechanismLayerMode.None` + `requireZeroPremiumAndSlashRates = true`, which yields zero mechanism topology addresses, empty initial mechanism factories, and enforces zero `budgetPremiumPpm`/`budgetSlashPpm` in stack deployment. Stream 6 assumptions: factory/preset wiring must compose these modes explicitly, must tolerate zero `allocationMechanism` / `allocationMechanismArbitrator` addresses without expecting mechanism events or registry deployment, must wire managed `budgetPremiumPpm = 0` and `budgetSlashPpm = 0`, and must keep the `premiumEscrow` naming/ABI intact rather than renaming to a generic risk-module surface. | 2026-03-11 |
| managed-authority-rotation-fix | Hard-cut managed child-flow admin identity to `ManagedBudgetController` so Safe authority rotation cannot split control across existing child flows/strategies | `agent-docs/exec-plans/active/2026-03-11-managed-authority-rotation-fix.md`; `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`; `src/goals/ManagedBudgetController.sol`; `src/goals/ManagedBudgetControllerStackDeployer.sol`; `src/interfaces/IManagedBudgetControllerStackDeployer.sol`; `test/goals/ManagedBudgetController.t.sol`; `test/goals/ManagedBudgetControllerStackDeployer.t.sol`; `test/goals/GoalFactorySpendPolicyDeploy.t.sol`; `ARCHITECTURE.md`; `agent-docs/cobuild-protocol-architecture.md`; `agent-docs/product-specs/protocol-lifecycle-and-invariants.md`; `agent-docs/references/module-boundary-map.md`; `agent-docs/references/goal-funding-and-reward-map.md` | remove unused `authority` arg from managed `prepareBudgetStack`; change managed child-flow `recipientAdmin` wiring from Safe to controller; update managed preset/docs to treat controller as both child allocator/admin identity | Depends on active managed-preset streams already freezing controller-owned allocator identity. Do not touch open-preset `BudgetTCR` stack paths or revert unrelated dirty files. Managed fix should be a hard cutover because repo docs still say there are no live deployments yet. | 2026-03-11 |
| managed-preset-implementation-hoisting | Hoist reusable managed preset deployments into `GoalFactory`, make `SingleAllocatorStrategy` clone-safe, and bootstrap managed goals from shared infra plus per-goal clones | `agent-docs/exec-plans/active/2026-03-11-managed-preset-implementation-hoisting.md`; `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`; `src/allocation-strategies/SingleAllocatorStrategy.sol`; `src/goals/GoalFactory.sol`; `src/goals/library/GoalFactoryManagedPresetDeploy.sol`; `test/flows/SingleAllocatorStrategy.t.sol`; `test/flows/FlowLedgerChildSyncProperties.t.sol`; `test/goals/ManagedBudgetController.t.sol`; `test/goals/GoalFactorySpendPolicyDeploy.t.sol`; `test/goals/GoalFactoryUnderwritingSlashConfigGuard.t.sol` | add `SingleAllocatorStrategy.initialize`; add `GoalFactory` managed preset immutable/shared deployment addresses; delete `SingleAllocatorStrategy.owner`, `SingleAllocatorStrategy.changeAllocator`; change `SingleAllocatorStrategy` constructor to `(goalTreasury, allocator)` direct-init / zero-zero implementation mode; update managed preset bootstrap to clone controller + strategy and reuse shared gate-policy / stack-deployer infra; update goal-factory guard test harness to provide a real `BudgetTCRFactory` metadata surface for the new constructor preflight | Depends on frozen managed-preset boundaries in `2026-03-11-managed-goal-preset-master-plan.md`. This task intentionally folds the active single-allocator immutable cut into the deployment-hoisting change: preserve controller allocator identity semantics, avoid `BudgetSingleAllocatorStrategy` edits unless compile fallout forces them, and do not overwrite unrelated in-flight work outside the listed files. | 2026-03-11 |
| codex-managed-removal-fail-closed | Add a controller-only budget-removal fail-close path and use it for managed-budget removals | `agent-docs/exec-plans/active/2026-03-11-managed-budget-removal-fail-closed.md`; `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`; `src/interfaces/IBudgetTreasury.sol`; `src/goals/BudgetTreasury.sol`; `src/goals/ManagedBudgetController.sol`; `src/interfaces/IManagedBudgetController.sol`; `test/goals/BudgetTreasury.t.sol`; `test/goals/ManagedBudgetController.t.sol`; architecture/spec docs if behavior text changes | add controller-only removal terminalization entrypoint on `IBudgetTreasury`/`BudgetTreasury`; update managed removal flow to call it for activated removals; update managed tests that currently expect unresolved activated removals | Preserve current unrelated `BudgetTreasury` / `ManagedBudgetController` edits already in the worktree. Scope this stream to managed removals for now; open `BudgetTCR` activation-locked removal semantics stay unchanged here to avoid unreviewed slash-behavior changes. | 2026-03-11 |
| codex-simplify-managed-removal-fail-closed | Run simplify pass for the managed-budget removal fail-close stream | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`; `src/interfaces/IBudgetTreasury.sol`; `src/goals/BudgetTreasury.sol`; `src/goals/ManagedBudgetController.sol`; `test/goals/BudgetTreasury.t.sol`; `test/goals/ManagedBudgetController.t.sol`; `ARCHITECTURE.md`; `agent-docs/product-specs/protocol-lifecycle-and-invariants.md`; `agent-docs/cobuild-protocol-architecture.md`; `agent-docs/references/goal-funding-and-reward-map.md`; `agent-docs/exec-plans/active/2026-03-11-managed-budget-removal-fail-closed.md` | simplify-only review; no symbol additions/renames/deletions planned unless a behavior-preserving cleanup is clearly safe | Respect existing in-flight edits in the main fail-close stream and do not widen scope beyond the listed files. | 2026-03-11 |
## Rules

1. Add a row before your first code edit for every coding task (single-agent and multi-agent).
2. Update your row immediately when scope or symbol-change intent changes.
3. Before deleting or renaming a symbol, check this table for dependencies.
4. Delete your row as soon as the task is complete or abandoned.
5. Leave only the header and empty table when there is no active work.
