# Coordination Ledger (Active Only)

Use this file only for currently active coding work. Keep it minimal and current.

## Open Entries

| Agent/Session | Task | Files in Scope | Symbols (add/rename/delete) | Dependency Notes | Updated (YYYY-MM-DD) |
| --- | --- | --- | --- | --- | --- |
| codex-main | Orchestrate budget-stack architecture cleanup workers | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `agent-docs/exec-plans/active/2026-03-12-budget-stack-architecture-workers.md`, worker prompt files/log references, parent integration across worker-owned files | Add active architecture-worker plan doc; reserve worker ownership for open prune guard, generic deploy seam, and managed-lane convergence; parent-only integration/verification/commit | Parent orchestration only; child workers must not commit and must fail closed on unrelated dirty files or ownership conflicts | 2026-03-12 |
| codex-worker-arch-b-generic-seam | Refactor shared budget-stack deploy seam to neutral treasury config and generic deployer surface | `src/interfaces/IBudgetStackDeployer.sol`, `src/interfaces/IBudgetStackChildFlowStrategyFactory.sol`, `src/tcr/interfaces/IBudgetTCRDeployer.sol`, `src/tcr/BudgetTCRDeployer.sol`, `src/tcr/library/BudgetTCRStackDeploymentLib.sol`, `src/tcr/library/BudgetTCRStackActions.sol`, open deployer tests (`test/BudgetTCRDeployments.t.sol`, `test/BudgetTCRManagedStackDeployments.t.sol`, related mocks/helpers) | Add neutral deployer/config interface; rename shared child-strategy factory surface; replace listing-shaped deploy seam with neutral treasury/risk init config; add premium-escrow reuse mode if needed for stateless null escrow support | Owns generic deployer/interface changes; managed convergence worker must wait for this lane before editing overlapping deployer-interface call sites | 2026-03-12 |
| codex-worker-arch-c-managed-converge | Collapse managed lane onto generic stack deployer and delete managed-only deployer path | `src/goals/ManagedBudgetController.sol`, `src/goals/GoalFactory.sol`, `src/goals/library/GoalFactoryManagedPresetDeploy.sol`, `src/goals/NullPremiumEscrow.sol`, `src/allocation-strategies/BudgetSingleAllocatorStrategyFactory.sol`, `src/interfaces/IManagedBudgetControllerStackDeployer.sol`, `src/goals/ManagedBudgetControllerStackDeployer.sol`, managed tests/docs touching those files | Add managed child-strategy factory; make `NullPremiumEscrow` stateless/shared-safe; switch managed controller/factory to generic stack deployer clone/config flow; delete managed-only deployer interface/contract if fully unused | Must build on generic deploy seam output from worker `arch-b`; do not modify `BudgetTCRDeployer.sol` directly unless parent explicitly reassigns scope after worker `arch-b` settles | 2026-03-12 |
| codex-main | Orchestrate codex-4 simplification batch for targets 1 and 4 | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `agent-docs/exec-plans/active/2026-03-12-simplification-worker-batch-14.md` | Add batch-14 plan doc; reserve active worker ownership rows for spend-policy helper and managed active-state derivation; no production-symbol changes in parent lane | Parent-managed orchestration only; child workers must not edit ledger unless scope must change and should otherwise report blockers | 2026-03-12 |
| codex-worker-t1-spend-policy | Unify duplicated spend-policy validation probe behind one shared helper | `src/goals/TreasuryBase.sol`, `src/goals/GoalFactory.sol`, `src/tcr/library/BudgetTCRInitValidation.sol`, optional shared helper library, related tests | Add shared spend-policy validation helper; replace duplicated probe logic at three call sites; preserve caller-specific custom errors | Safe to run now that the old GoalFactory/BudgetTCR wiring lanes are clean; keep scope off unrelated deploy/controller refactors | 2026-03-12 |
| codex-worker-t4-managed-active-derived | Derive managed budget active state from index mapping instead of storing duplicate bool | `src/goals/ManagedBudgetController.sol`, `src/interfaces/IManagedBudgetController.sol`, related managed-controller tests | Delete stored `BudgetDeployment.active` field if safe; derive `active` from `_activeBudgetIndexPlusOne`; update views/tests consistently | Keep this lane narrowly on derived state; do not fold in topology helper dedupe or controller-runtime extraction | 2026-03-12 |
## Rules

1. Add a row before your first code edit for every coding task (single-agent and multi-agent).
2. Update your row immediately when scope or symbol-change intent changes.
3. Before deleting or renaming a symbol, check this table for dependencies.
4. Delete your row as soon as the task is complete or abandoned.
5. Leave only the header and empty table when there is no active work.
