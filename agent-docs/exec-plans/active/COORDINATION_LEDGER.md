# Coordination Ledger (Active Only)

Use this file only for currently active coding work. Keep it minimal and current.

## Open Entries

| Agent/Session | Task | Files in Scope | Symbols (add/rename/delete) | Dependency Notes | Updated (YYYY-MM-DD) |
| --- | --- | --- | --- | --- | --- |
| codex-low-impact-cleanups | Validate and apply low-impact protocol simplifications | `src/goals/GoalTreasury.sol`, `src/library/FlowPools.sol`, `src/Flow.sol`, `src/teamflow/TeamFlow.sol`, `agent-docs/exec-plans/active/2026-03-10-low-impact-protocol-cleanups.md` | delete `_raisedForLifecycle`; delete `removeFromPools`; preserve `recordHookFunding` unless safe removal becomes proven | Avoid interface changes unless usage analysis proves they are safe; no behavior change intended for goal min-raise or recipient removal paths | 2026-03-10 |
| codex-ledger-delta-hardening | Make budget-delta detection ledger-owned and fail-closed on malformed order | `src/goals/BudgetStakeLedger.sol`, `src/interfaces/IBudgetStakeLedger.sol`, `src/hooks/GoalFlowAllocationLedgerPipeline.sol`, `src/library/GoalFlowLedgerMode.sol`, `test/goals/BudgetStakeLedger*.t.sol`, `test/flows/GoalFlowLedgerMode*.t.sol`, `test/flows/FlowLedgerChildSyncProperties.t.sol`, `test/harness/GoalFlowLedgerModeHarness.sol`, `ARCHITECTURE.md`, `agent-docs/cobuild-protocol-architecture.md`, `agent-docs/exec-plans/active/2026-03-10-ledger-owned-budget-delta-validation.md` | add ledger preview/delta-return APIs; delete direct pipeline/library delta-detection path; add local malformed-order enforcement on ledger merge path | Preserve decreases-first ordering because child-sync execution is gas-budgeted and tests lock that behavior; do not touch files owned by other active entries | 2026-03-10 |
| codex-coverage-ledger-delta-audit | Audit ledger-owned delta refactor for missing high-impact tests and add one parity fuzz if needed | `test/goals/BudgetStakeLedgerCoverageCutover.t.sol` | add preview/checkpoint parity fuzz only; no production symbol changes | Narrow audit-only touch on top of active ledger-delta work; do not revert concurrent topology-coverage edits in the same suite | 2026-03-10 |
| codex-factory-topology-error-granularity | Split coarse factory/topology failures into probe-specific deployment and registration errors | `src/rounds/RoundFactory.sol`, `src/teamflow/TeamFlowFactory.sol`, `src/goals/BudgetStakeLedger.sol`, `src/interfaces/IBudgetStakeLedger.sol`, `test/rounds/RoundFactory.t.sol`, `test/teamflow/TeamFlowFactory.t.sol`, `test/goals/BudgetStakeLedgerCoverageCutover.t.sol`, `agent-docs/exec-plans/active/2026-03-10-factory-topology-error-granularity.md` | add factory context probes + richer custom errors; refine `INVALID_BUDGET_TOPOLOGY` payload with topology probes; no symbol deletes planned | Limit `BudgetStakeLedger` edits to registration/topology error reporting only; coordinate with the active `RoundFactory` refactor claim by avoiding unrelated structural changes and do not touch the in-flight ledger delta-preview work | 2026-03-10 |
| codex-community-system-floors | Add floor-first community routing so system goals receive guaranteed treasury funding before discretionary routing | `src/tcr/interfaces/ICommunityGoalRegistry.sol`, `src/tcr/CommunityGoalRegistry.sol`, `src/interfaces/ICobuildSplitHook.sol`, `src/hooks/CobuildSplitHook.sol`, `test/tcr/CommunityGoalRegistry.t.sol`, `test/hooks/CobuildSplitHook.t.sol`, `test/juicebox/CobuildPaymentTerminalCoreIntegration.t.sol`, `ARCHITECTURE.md`, `agent-docs/cobuild-protocol-architecture.md`, `agent-docs/product-specs/protocol-lifecycle-and-invariants.md`, `agent-docs/references/goal-funding-and-reward-map.md`, `agent-docs/RELIABILITY.md`, `agent-docs/exec-plans/active/2026-03-10-community-system-floors.md` | add `floorPpm`/system-route registry views and total accounting; add split-hook floor routing helpers and discretionary-only backlog/history semantics; no symbol deletes planned | Shared-file overlap with active `CobuildSplitHook` refactor row: preserve current in-flight helper extraction and do not revert unrelated hook/test edits; historical observed volume must remain discretionary-only | 2026-03-10 |
## Rules

1. Add a row before your first code edit for every coding task (single-agent and multi-agent).
2. Update your row immediately when scope or symbol-change intent changes.
3. Before deleting or renaming a symbol, check this table for dependencies.
4. Delete your row as soon as the task is complete or abandoned.
5. Leave only the header and empty table when there is no active work.
