# Coordination Ledger (Active Only)

Use this file only for currently active coding work. Keep it minimal and current.

## Open Entries

| Agent/Session | Task | Files in Scope | Symbols (add/rename/delete) | Dependency Notes | Updated (YYYY-MM-DD) |
| --- | --- | --- | --- | --- | --- |
| codex-gpt5-juror-router-clone-migration-2026-03-04 | Migrate per-goal `JurorSlasherRouter` from direct `new` deployment to implementation+clone initialization wiring; simplify pass limited to behavior-preserving clarity cleanup | `src/goals/JurorSlasherRouter.sol`, `src/goals/library/GoalFactoryCoreStackDeploy.sol`, `src/goals/GoalFactory.sol`, `script/DeployGoalFactoryImplementations.s.sol`, `script/DeployGoalFactory.s.sol`, `test/goals/GoalFactoryUnderwritingSlashConfigGuard.t.sol`, `test/goals/JurorSlasherRouter.t.sol`, `agent-docs/exec-plans/active/2026-03-04-juror-router-clone-migration.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add `GoalFactory.JUROR_SLASHER_ROUTER_IMPL`; add `CoreStackRequest.jurorSlasherRouterImpl`; add `JurorSlasherRouter.initialize`; replace constructor-arg deployment callsites with clone+initialize flow | Preserve `authority()==BudgetTCRFactory` and `stakeVault()==per-goal StakeVault` invariants used by `BudgetTCRFactory` validation; keep one-time slasher wiring in `GoalTreasury.initialize`; avoid reverting concurrent row-9 edits in shared script/test files | 2026-03-04 |
| codex-gpt5-flow-bulk-remove-cutover-2026-03-04 | Hard cutover: remove `bulkRemoveRecipients` entrypoint from `Flow` | `src/Flow.sol`, `test/flows/FlowInitializationAndAccessAccess.t.sol`, `test/flows/FlowRecipients.t.sol`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | delete `Flow.bulkRemoveRecipients`; delete/retarget tests that call removed selector | Hard cutover with no backward-compat shim; keep recipient removal behavior via `removeRecipient` | 2026-03-04 |
| codex-gpt5-mechanism-policy-funding-pool-only-2026-03-04 | Ensure allocation-mechanism funding policy tracks only pool cumulative receipts (not raw escrow balance) so min/deadline/cap policy cannot be satisfied by temporary escrow top-ups | `src/tcr/AllocationMechanismTCR.sol`, `test/rounds/AllocationMechanismTCR.t.sol`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | refactor `_effectiveEscrowFunding` semantics to pool-only accounting; update direct-escrow funding tests to new policy expectations; add regression coverage for pre-deadline sync not inflating `maxEffectiveFundingObserved` from raw escrow balance | Preserve refund/release lifecycle and trusted factory allowlist behavior while removing balance-inflation policy bypass | 2026-03-04 |
| codex-gpt5-goalfactory-withdrawal-gas-model-2026-03-04 | One-off gas profiling: deploy goal via `GoalFactory`, append many registered budgets, measure `prepareUnderwriterWithdrawal`/withdraw gas and griefing thresholds | `test/goals/tmp/GoalFactoryWithdrawalGasModel.t.sol` (temporary), `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add temporary gas-profile harness/contracts (`GoalFactoryWithdrawalGasModelTest`, mock revnet/tokens/rulesets/budget-tcr-factory, budget treasury/escrow stubs); no production symbol changes | Scope is benchmark-only, temporary file deleted after measurements; avoid touching active ownership rows/files above | 2026-03-04 |
| codex-gpt5-budget-credit-cap-helper-extraction-2026-03-06 | Extract repeated best-effort goal-flow helper logic from `BudgetTCRCreditCapActions.bestEffortEnforceBudgetCreditCap` without changing failure semantics | `src/tcr/library/BudgetTCRCreditCapActions.sol`, `test/BudgetTCRCreditLineGating.t.sol`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add `_tryLoadReceived`; add `_bestEffortSetRecipientEnabled`; refactor `_bestEffortDisableRecipientWhenRunwayExceeded` and `bestEffortEnforceBudgetCreditCap` to reuse helpers; add regression tests for received-read and recipient-toggle failure events | Preserve exact `BudgetCreditCapEnforcementFailed` target/selector semantics and current early-return behavior; avoid touching unrelated in-flight TCR topology edits | 2026-03-06 |
| codex-gpt5-budget-tcr-terminal-wrapper-removal-2026-03-06 | Remove the pure forwarding terminal-action wrappers from `BudgetTCR` and call the terminal-actions library directly at the existing use sites | `src/tcr/BudgetTCR.sol`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | delete `BudgetTCR._resolveBudgetTerminalStateBestEffort`; delete `BudgetTCR._resolveBudgetTerminalStateStrict`; delete `BudgetTCR._removeRecipientFromGoalFlowIfPresent`; delete `BudgetTCR._trySyncGoalTreasury` | Touches a file with concurrent topology-registry edits; keep changes tightly scoped to the terminal-action call sites and do not disturb in-flight registry/storage work | 2026-03-06 |
| codex-gpt5-mock-budget-tcr-system-removal-2026-03-06 | Remove `test/mocks/MockBudgetTCRSystem.sol` by cutting remaining BudgetTCR suites over to real fixtures or suite-local harnesses | `test/mocks/MockBudgetTCRSystem.sol`, `test/helpers/BudgetTCRSystemHarnesses.sol`, `test/BudgetTCR.t.sol`, `test/BudgetTCRFlowRemovalLiveness.t.sol`, `test/BudgetTCRCreditLineGating.t.sol`, `test/BudgetTCRManagerRewardPoolWiring.t.sol`, `test/BudgetTCRBudgetTreasuryInvariant.t.sol`, `test/BudgetTCRFactory.t.sol`, `src/tcr/library/BudgetTCRStackActions.sol`, `agent-docs/exec-plans/active/2026-03-06-mock-budget-tcr-system-removal.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | delete `MockBudgetTCRSystem.sol`; replace imports/usages with real local flow/treasury fixtures or suite-local minimal stubs; if verification hits active shared-config refactor fallout, layer the smallest compatible compile fix on touched budget-stack config/init call sites without reverting that refactor | `test/BudgetTCR.t.sol` is concurrently owned by topology-registry work; `src/tcr/library/BudgetTCRStackActions.sol` plus `test/BudgetTCRFactory.t.sol` are concurrently affected by shared-config canonicalization; keep any layered fix narrowly on config/init typing and do not disturb topology behavior; keep Cobuild suites out of scope | 2026-03-06 |
| codex-gpt5-tcr-shared-config-canonicalization-2026-03-06 | Canonicalize shared `GeneralizedTCR` registry init/config types and replace duplicated near-identical wrappers through the budget/mechanism/round deployment path | `src/tcr/interfaces/IGeneralizedTCR.sol`, `src/tcr/GeneralizedTCR.sol`, `src/tcr/AllocationMechanismTCR.sol`, `src/tcr/RoundSubmissionTCR.sol`, `src/tcr/interfaces/IBudgetTCR.sol`, `src/tcr/BudgetTCR.sol`, `src/tcr/BudgetTCRFactory.sol`, `src/tcr/library/BudgetTCRStackActions.sol`, `src/rounds/RoundFactory.sol`, `src/goals/library/GoalFactoryBudgetTcrDeploy.sol`, `src/goals/GoalFactory.sol`, `test/rounds/*.t.sol`, `test/BudgetTCRFactory*.t.sol`, `test/goals/GoalFactoryBudgetTcrDeploy.t.sol`, `agent-docs/references/tcr-and-arbitration-map.md`, `agent-docs/exec-plans/active/2026-03-06-tcr-shared-config-canonicalization.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add canonical shared `IGeneralizedTCR` config structs; change `GeneralizedTCR.__GeneralizedTCR_init` to struct-based init; refactor concrete `RegistryConfig` wrappers to embed shared config; delete `GoalFactoryBudgetTcrDeploy.RegistryConfigArgs`; rename `RoundFactory.SubmissionTcrConfig` to a shared-config-derived shape if needed | `src/tcr/BudgetTCR.sol`, `src/tcr/interfaces/IBudgetTCR.sol`, and `src/tcr/library/BudgetTCRStackActions.sol` already have concurrent edits; keep refactor strictly on config/init surfaces and do not disturb topology-registry behavior | 2026-03-06 |
## Rules

1. Add a row before your first code edit for every coding task (single-agent and multi-agent).
2. Update your row immediately when scope or symbol-change intent changes.
3. Before deleting or renaming a symbol, check this table for dependencies.
4. Delete your row as soon as the task is complete or abandoned.
5. Leave only the header and empty table when there is no active work.
